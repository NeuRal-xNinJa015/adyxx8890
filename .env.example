# ─────────────────────────────────────────────
# ADYX Platform — Environment Configuration
# ─────────────────────────────────────────────

# Server port (default: 8443)
PORT=8443

# Environment: "production" or "development"
# production = minimal logging, static file caching, gzip
# development = verbose logging, no caching
NODE_ENV=production
